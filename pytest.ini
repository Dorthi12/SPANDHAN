[pytest]
# Discard all tmp_path directories immediately after each test.
# This prevents pytest from creating/managing the 'pytest-current'
# directory junction on Windows, which requires admin rights to unlink
# and causes a PermissionError during session teardown.
tmp_path_retention_policy = none
